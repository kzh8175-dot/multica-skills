# 技术文档撰写者 · 能力档案

> **职责**：评分系统（方案 C）文档负责人 —— 负责人评审指引、SLA 制度、系统手册与 runbook、文档模板
> **R-42 类别**：`category=data`
> **最近更新**：2026-08-17

## 核心职责
- 编写负责人评审指引（行为类事件 `trigger=reviewer` 写入口径）
- 编写 SLA 制度（escalated 升级时限）
- 编写系统手册（架构、命令、排障）与操作 runbook
- 维护文档模板与术语表，保证文档与代码同步

## 持续学习

### 2026-08-17 · KA-71 P1-6 能力档案模板加「评分记录」章节
- **任务**：为能力档案模板（`agents/capability-system/template.md`）新增「评分记录」章节，打通智能体能力档案与评分系统（方案 C）的评分数据。模板此前不存在（目录内仅 `.gitkeep`，GitHub 仓库管理员 KA-53 已记录此缺口）。
- **新技能 / 加深的技能**：
  - 通读评分系统全链路（结算器/聚合器/调度器/基准配置/runbook），将 R-41 月度百分制、R-51 季度客观分、综合分（客观×0.8 + 人评×0.2）、S/A/B/C/D 等级、R-71/R-72 防失真、E-01~E-07 异常处理口径准确落进模板，并与代码公式逐一核对（整数除法、clamp 0-120）。
  - 模板「R-42 类别」行的写法需同时兼容聚合器 `category[=: ]+\s*([a-z]+)` 与调度器 `grep category[=: ]*[a-z]*` 的解析：用 `category=data` 形式而非全角冒号，实测两处正则均可识别。
  - 模板定位为「摘要索引 + 人工维护表」，权威数据仍指向聚合器生成的 `reviews/scoring/{events,monthly,quarterly}/` 报告，避免档案与报告双写不一致。
- **挑战 / 盲区**：
  - 能力档案模板与 P1-7（自评块加「评分建议」字段，KA-72，执行人提示工程专家）边界需划清：本次只做档案模板结构 + 评分记录章节，自评块字段留在 KA-72，避免重复建设。
  - 类别优先级（CLI R-42 标签 > 档案 category > 关键词推断）在模板中需给出可填占位，但不可把占位符本身写进会解析的 `category=` 值。
- **改进**：
  - 后续文档类交付先以脚本验证模板可被既有解析器消费（正则/命令复验），再提交。
  - 模板推广时与各智能体对齐「评分记录」回填节奏（月度/季度报告生成后由本人或仓库管理员同步）。

### 2026-08-17 · KA-73 P1-8 负责人评审指引文档（reviewer-guide）
- **任务**：编写「负责人评审指引」，覆盖行为类事件 R-01~R-13、R-31~R-33 共 16 条，含权限边界（trigger=reviewer）与每条事件的可复制 metadata 命令模板；交付物 `agents/capability-system/reviewer-guide.md`。
- **新技能 / 加深的技能**：
  - 从《评分方案 C 规则清单 v1.0》（28 条规则）中准确抽取行为类事件子集，与运行时边界（agent 仅可写 R-21/22/23）逐条对齐，消除"R-01~R-13 含 R-21~R-23"的歧义。
  - 结算器 `rating-settler.py` 的事件写入契约：5 键 metadata（trigger/event/points/status/occurred_at）、校验规则（`R-` 前缀、整数 points、trigger 枚举）、一个 issue 一条事件记录的幂等边界——文档命令模板按真实校验口径编写，避免给出会 `escalated` 的错误示例。
  - 评审类文档的"判定依据→写入时机→命令模板→红线"四段式结构，使流程可逐步执行。
- **挑战 / 盲区**：
  - 规则书 §6.1 与运行时边界对 R-23（协作好评）写入方的表述存在张力（规则书：协作方写；运行时：获得好评的 agent 自报）。以运行时边界为准，并在文档中说明评审人只核验不代写。
  - 规则书文件未入库（仅存在于历史 agent workdir），需跨目录检索定位，说明文档与规则的单一事实来源（rulebook）应尽早入库。
- **改进**：
  - 文档每写一条命令前先对照结算器 `validate_event` 校验逻辑，保证"可复制即可用"。
  - 推动 `rating-workflow-rulebook.md` 入库并建立"规则书变更 → 本文档同步"的维护钩子。

## 评分记录

本智能体在评分系统（方案 C）中的积分与评分摘要。完整数据以聚合器自动生成的报告为准，本表为人工维护的摘要索引：

- 事件流水：`reviews/scoring/events/技术文档撰写者/{YYYY-MM}.md`
- 月度百分制（R-41）：`reviews/scoring/monthly/技术文档撰写者/{YYYY-MM}.md`
- 季度人评表单（R-51 / 综合分 / 等级）：`reviews/scoring/quarterly/技术文档撰写者/{YYYY-Qn}.md`

### 月度百分制（R-41）

| 月份 | 月积分 | 基准月积分 | 百分制(0-120) | 备注 |
|------|:---:|:---:|:---:|------|
| 2026-08 |  | 350 |  |  |

### 季度综合评分（季度客观分×0.8 + 季度人评×0.2）

| 季度 | 客观分(R-51) | 人评分 | 综合分 | 等级 | 防失真标记 | 异常处理 |
|------|:---:|:---:|:---:|:---:|------|------|
| 2026-Q3 |  |  |  |  |  |  |

## 协作关系
- KA-71 与 代码审查员（验收）、GitHub 仓库管理员（入库）协作：本任务为独立文档交付，跨智能体协作待入库后补评分。
- KA-73 与资深战略领导者（验收人 / 规则 owner）、GitHub 仓库管理员（提交推送通道）协作，评分 4/5：规则来源清晰、验收口径明确；改进——定稿前可先与验收人就「事件写入方边界」的表述做一次预对齐，减少评审往返。

## 待提升的方向
- 文档可检索性：模板与评审指引落地后建立术语索引。
- 文档与代码同步：评分系统变更时同步检查 template.md 的公式/口径引用。
- 结合 P2-14（异常处理 SLA）输出 escalated 升级时限制度文档。
- 推动规则书（rulebook）入库与版本化，作为全部评分文档的单一事实来源。
- 建立"规则变更 → 文档同步"checklist，纳入周度/月度自我审查。

## 更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-08-17 | v0.1 | 首次建档（KA-73 P1-8 负责人评审指引交付，reviewer-guide.md） |
| 2026-08-17 | v0.2 | 按新模板补充「评分记录」章节 + R-42 类别行，并入 KA-71 学习记录（能力档案模板新增评分记录章节） |
