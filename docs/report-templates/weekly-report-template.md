# 系统监控周报（模板）

> **报告类型**：周报
> **周期编号**：`{YYYY-Wxx}`（ISO 周编号）
> **生成时间**：`{YYYY-MM-DD}T{HH}:00:00Z`
> **生成方**：SRE稳定性工程师 + 数据可视化工程师（P2-13）
> **数据来源**：三任务日志 / autopilot runs / `audit-events.py` / 流水完整性对账
> **模板**：`docs/report-templates/weekly-report-template.md`（依据 `docs/system-report-spec.md`）
> **口径基准**：`docs/runbook.md` §6「监控与周报」

---

## 一、关键指标摘要（KPI）

| 指标 | 本周值 | 上周值 | 目标 | 状态 |
|------|:---:|:---:|:---:|:---:|
| 三任务 on-time 执行率 | `%` | `%` | >99% | ✅/⚠️/🔴 |
| 每日结算失败数 | `n` | `n` | 0 | ✅/⚠️/🔴 |
| 月末聚合失败数 | `n` | `n` | 0 | ✅/⚠️/🔴 |
| 季度人评触发失败数 | `n` | `n` | 0 | ✅/⚠️/🔴 |
| escalated 事件数 | `n` | `n` | 0 | ✅/⚠️/🔴 |
| pending 滞留 >48h 数 | `n` | `n` | 0 | ✅/⚠️/🔴 |
| 流水完整性（跨文件重复 issue） | `n` | `n` | 0 | ✅/⚠️/🔴 |

> 说明：on-time 执行率 = 按 cron 计划窗口内完成数 / 应执行数（目标 >99%，对应月错误预算 ≈1 次失火）。

## 二、任务执行明细

### 2.1 每日结算（00:30）

- 应执行 `n` 次 / 成功 `n` 次 / 失败 `n` 次 / 静默失火 `n` 次
- 失败明细：`{日期}` `{失败原因}`（日志位置：`<生产>/logs/settlement/{YYYY-MM-DD}.log`）

### 2.2 月末聚合（月末最后一天）

- 本月应执行：`n` 次 / 结果：`{成功/失败/未到窗口}`
- 说明：`{补充}`

### 2.3 季度人评触发（季末窗口）

- 本周窗口状态：`{未到窗口 / 已触发 n 次}`
- 说明：`{补充}`

## 三、流水完整性对账

```bash
python3 scripts/audit-events.py --month {YYYY-MM}
```

- 跨文件重复 issue：`0`（若 >0 立即 `--reconcile` 并复盘）
- 未知智能体行：`n` 行（仅「未分配 issue」清单）

## 四、风险与异常

| # | 类型 | 事件 | 影响 | 处置 | 状态 |
|---|------|------|------|------|:---:|
| 1 | L1 脚本失败 | `{描述}` | `{影响}` | `{处置}` | 处置中/已闭环 |
| 2 | L2 升级（24h 未处置） | `{描述}` | `{影响}` | 升级资深战略领导者（SLA 48h） | 处置中/已闭环 |
| 3 | L3 静默失火 | `{描述}` | `{影响}` | 补触发 + 告警（静默 = 故障） | 处置中/已闭环 |
| 4 | escalated 事件 | `{R-XX}` | `{影响}` | `{升级路径}` | 处置中/已闭环 |

> 升级路径与时限：`docs/runbook.md` §3；P2-14 异常处理 SLA 交付后以此为准。

## 五、结论与行动项

| # | 行动项 | 负责人 | 截止 | 状态 |
|---|--------|--------|------|:---:|
| 1 | `{行动项}` | `{角色}` | `{YYYY-MM-DD}` | 进行中 |
| 2 | `{上期行动项回执}` | `{角色}` | — | done / 未完成原因 |

## 六、归档信息

- 归档：GitHub Release（tag `reports-{YYYY}-Q{n}`）
- 本报告链接：`{Release URL}`
