# 角色类别 → 基准月积分 共享配置（方案C R-42）
#
# 供 review-scheduler.sh 与 rating-aggregator.py 共用：
#   - bash:  `source rating-benchmarks.conf` 后变量 execution/data/... 可用；
#   - python: 按 `KEY=VALUE` 解析（= 左右允许空白，行首 # 为注释）。
#
# 变更基准时只改本文件，两个组件同时生效。

execution=400
data=350
marketing=350
creative=300
technical=300
default=300
